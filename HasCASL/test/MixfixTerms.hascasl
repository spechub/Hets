- a !
a + a
not a = a
not a + a
not s a + a 
not s(n) <= 0 
def s(n) <= 0 
not a => b
not s a => s b
a + def a
def a = b
10 * b(n div 10)
10 when n < 10 else 10 * b(n div 10)
m(m,n) = m when m <= n else n
(i div p < j div q <=> i * q < j * p )
f + -f = f
-f + -f + -f = -f + -f + -f
