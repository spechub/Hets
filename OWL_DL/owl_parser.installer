#!/bin/bash

# A sample application using the OWL API. 

if [ -d $HETS_OWL_PARSER  ]
then
    java -jar $HETS_OWL_PARSER/java/OWLParser.jar $* || exit $?   
else
    java -jar $INSTALL_PATH/lib/hets/java/OWLParser.jar $* ||exit $? 
fi

