#!/bin/bash

# A sample application using the OWL API. 

## cd $HETS_OWL_PARSER
rm output.term 2>/dev/null
java -jar $INSTALL_PATH/lib/hets/OWLParser/java/OWLParser.jar $* || exit $?   
# mv output.term $HETS_OWL_PARSER
# cd -
