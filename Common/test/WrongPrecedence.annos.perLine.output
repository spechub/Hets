%prec( {a} < {b})%
%prec( {b} < {c})%
%prec( {c} < {a})%
%left_assoc( a, b, c)%
%right_assoc( b, a, d, d)%
parse error at (line 6, column 1):
unexpected end of input
expecting "%%", "%{", "%(" or "%"

