module Framework.WriteLogicUtils where

undef = error "Framework.WriteLogicUtils"

mkCompOpt = undef

multiOpt = undef

synOpt = undef

mkModDecl = undef

mkImports = undef

mkLid = undef

mkImpl = undef

mkInst = undef

mkDecl = undef
