module GenericSequent where

import AbstractSyntax
import qualified Data.List as List
import qualified Data.Set as Set

-- testing Segala
-- test = S (KD (Or (At (K (Or (At (S (KD T))) F))) (Or F (At (K F)))))::Segala 

